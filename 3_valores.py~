from mrjob.job import MRJob

class MRWordFreqCount(MRJob):

    def mapper(self, key, line):
        data = line.split(",")
        nombre = "BBVA"
        if data[0]==nombre:
            yield("Deberia poner BBVA -- > ", line)


            
if __name__ == '__main__':
    MRWordFreqCount.run()
